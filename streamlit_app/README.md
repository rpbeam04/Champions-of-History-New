# Streamlit App

This folder contains a minimal Streamlit app to preview and generate the tournament bracket.

Run locally (from the repo root) using the project's virtualenv:

```powershell
.venv\Scripts\python.exe -m streamlit run streamlit_app/app.py
```

The app writes generated HTML and PDF into the project's `outputs/` directory.
